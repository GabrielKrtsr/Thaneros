<section class="page-hero">
  <div class="container page-hero-grid">
    <div class="reveal">
      <p class="eyebrow"><%= eyebrow %></p>
      <h1><%= title %></h1>
    </div>
    <% if (description) { %>
      <p class="page-hero-copy reveal reveal-delay"><%= description %></p>
    <% } %>
  </div>
</section>
